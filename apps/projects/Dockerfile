# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @fubs/projects`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t fubs-projects`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system fubs-projects && \
          adduser --system -G fubs-projects fubs-projects

COPY apps/projects/dist fubs-projects/
COPY apps/projects/package.json fubs-projects/
RUN chown -R fubs-projects:fubs-projects .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix fubs-projects --omit=dev -f install

CMD [ "node", "fubs-projects" ]
